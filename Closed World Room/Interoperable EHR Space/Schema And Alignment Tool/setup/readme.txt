Before running the application necessary packages should be installed. 
In order to install those packages, run the following command:

> python install.py

additional packages - will be install manually
- wxPython
- pywin32 for windows environment